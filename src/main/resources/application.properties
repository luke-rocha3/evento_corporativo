# Nome da aplicação (opcional, só pra organização/logs)
spring.application.name=event-organizer

# Configuração do banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/sistema_eventos
spring.datasource.username=root
spring.datasource.password=24104425

# Configuração do JPA / Hibernate
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configuração de timezone (opcional, mas recomendado)
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver